<!DOCTYPE html>
<html>
<head>
    <title>Users</title>
</head>
<body>
    <div class="container">
        <h1>Users</h1>
        <% @users.each do |user| %>
        <div class="user-card">
            <%= link_to user.name, user_path(user) %>
        </div>
        <% end %>
    </div>
</body>
</html>